[package]
name = "dns-streaming"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = {version = "1.52.3", features = ['full']} 
ffmpeg-next = "8.1.0"
ffmpeg-sys-next = { version = "8.1.0", features = ["avcodec"] }
